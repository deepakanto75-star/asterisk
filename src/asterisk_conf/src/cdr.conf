[general]
enable=yes              ; Globally enable CDR logging
unanswered=yes          ; Log unanswered calls (optional, but good for completeness)
default_handler=my_cdr_handle ; <--- THIS IS THE KEY!